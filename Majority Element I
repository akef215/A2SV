class Solution(object):
    def majorityElement(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        freq = dict()
        out = []
        for i in nums:
            freq[i] = freq.setdefault(i, 0) + 1
        for i in freq:
            if freq[i] > len(nums) // 3:
                out.append(i)
        return out         
