class Solution: 
    def selectionSort(self, arr):
        #code here
        for i in range(len(arr)):
            m = arr[i]
            for j in range(i + 1, len(arr)):
                if m > arr[j]:
                    m, arr[j] = arr[j], m
            arr[i] = m
        return arr    
