class Solution(object):
    def thirdMax(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        nums = list(set(nums))
        nums.sort(key=lambda x : -x)
        if len(nums) >= 3:
            return nums[2]
        else:
            return nums[0]    
        
