class Solution(object):
    def minOperations(self, logs):
        """
        :type logs: List[str]
        :rtype: int
        """
        path = []
        for log in logs:
            if log == "../" and path:
                path.pop()
            elif log == "./":
                continue
            else:
                path.append(log)
        return len(path)  
