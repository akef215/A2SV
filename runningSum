class Solution(object):
    def runningSum(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        s = 0
        out = [0] * len(nums)
        for i in range(len(nums)):
            s += nums[i]
            out[i] = s
        return out 
